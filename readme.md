# Power calculator

## Prerequisites
* node > 16
* npm > 7

## Running
`npm run watch`

## Development
Uses StandardJS for linting. In VSCode you can install an extension for automatic linting. Otherwise